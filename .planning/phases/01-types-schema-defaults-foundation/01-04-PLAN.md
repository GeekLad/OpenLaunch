---
phase: 01-types-schema-defaults-foundation
plan: 04
type: execute
wave: 3
depends_on:
  - 01-02
  - 01-03
files_modified:
  - config/environment.ts
  - lib/db/service.ts
  - app/api/tokens/create/route.ts
autonomous: true
requirements:
  - PERS-01
  - PERS-03
must_haves:
  truths:
    - "Business defaults are removed from config/environment.ts and live in config/defaults.ts"
    - "TokenCreateInput interface includes all new launch parameter fields"
    - "API route accepts and passes all new fields to dbService.createToken"
    - "TypeScript compilation passes for all three modified files"
  artifacts:
    - path: "config/environment.ts"
      provides: "Infrastructure-only env config"
      does_not_contain: "TOTAL_SUPPLY"
      does_not_contain: "INITIAL_PRICE"
      does_not_contain: "PRICE_RANGE_MIN"
      does_not_contain: "PRICE_RANGE_MAX"
      does_not_contain: "FEE_DECAY_DURATION_MINUTES"
      does_not_contain: "FEE_DECAY_PERIODS"
      does_not_contain: "POOL_LIQUIDITY_PERCENTAGE"
    - path: "lib/db/service.ts"
      provides: "Updated TokenCreateInput with new fields"
      contains: "feeSchedulerMode"
      contains: "feeTokenMode"
      contains: "holdbackPercentage"
    - path: "app/api/tokens/create/route.ts"
      provides: "API accepting new launch parameters"
      contains: "feeSchedulerMode"
      contains: "feeTokenMode"
  key_links:
    - from: "config/defaults.ts"
      to: "lib/db/service.ts"
      via: "TokenCreateInput uses default values for optional fields"
    - from: "lib/db/service.ts"
      to: "app/api/tokens/create/route.ts"
      via: "dbService.createToken(tokenData)"
---

<objective>
Update backend files to consume the new types, defaults, and schema. Remove business defaults from environment.ts, extend the database service's create input, and update the API route to accept the new parameters.

Purpose: These files are the bridge between the new data layer (types, schema, defaults) and the existing launch flow. Without these updates, the backend cannot persist the new configurable parameters.
Output: Updated environment.ts, service.ts, and route.ts that compile and align with the new schema.
</objective>

<execution_context>
@/home/geeklad/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/01-types-schema-defaults-foundation/01-CONTEXT.md
@.planning/phases/01-types-schema-defaults-foundation/01-RESEARCH.md
@.planning/phases/01-types-schema-defaults-foundation/01-PATTERNS.md
@config/environment.ts
@lib/db/service.ts
@app/api/tokens/create/route.ts
@lib/db/schema/tokens.ts
</context>

<interfaces>
From config/environment.ts (current — keys to REMOVE):
- TOTAL_SUPPLY, INITIAL_PRICE, PRICE_RANGE_MIN, PRICE_RANGE_MAX
- FEE_DECAY_DURATION_MINUTES, FEE_DECAY_PERIODS
- POOL_LIQUIDITY_PERCENTAGE

Keys to KEEP in environment.ts:
- APP_NAME, RPC_URL, SOLANA_NETWORK, QUOTE_TOKEN_MINT (infrastructure)
- TOKEN_DECIMALS, IPFS_GATEWAY, MAX_IMAGE_SIZE_MB, METADATA_MUTABLE
- LAUNCHPAD_URL, FEE_UPDATE_INTERVAL_* (cron intervals)
- ENABLE_FEES_DISPLAY

From lib/db/service.ts (current TokenCreateInput — fields to ADD):
- priceRangeMin: number
- priceRangeMax: number
- feeSchedulerMode: string
- feeTokenMode: string
- startingMarketCap: string
- endingMarketCap: string
- startRate: number
- endRate: number
- durationMinutes: number
- fixedBaseFeeBps: number
- holdbackPercentage: number

From app/api/tokens/create/route.ts (current request body parsing):
- Currently parses: mintAddress, poolAddress, name, symbol, description, logoUrl, metadataUri, decimals, totalSupply, initialPrice, quoteTokenMint, poolLiquidityPercentage, feeDecayDurationMinutes, feeDecayPeriods, launchDate, launchSlot, tx signatures, creatorWallet
- Must add parsing for all new fields listed above
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Remove business defaults from config/environment.ts</name>
  <read_first>
    - config/environment.ts (current file)
    - config/defaults.ts (from Plan 02 — the new defaults file)
    - 01-CONTEXT.md (D-10: defaults hardcoded in TypeScript, no env vars for business defaults)
  </read_first>
  <files>config/environment.ts</files>
  <action>
    Modify `config/environment.ts` to remove all business defaults per D-10. Specifically:
    1. REMOVE these fields from the `ENV` object:
       - `TOTAL_SUPPLY`
       - `INITIAL_PRICE`
       - `PRICE_RANGE_MIN`
       - `PRICE_RANGE_MAX`
       - `FEE_DECAY_DURATION_MINUTES`
       - `FEE_DECAY_PERIODS`
       - `POOL_LIQUIDITY_PERCENTAGE`
    2. KEEP all infrastructure and feature-flag fields:
       - `APP_NAME`, `RPC_URL`, `SOLANA_NETWORK`
       - `QUOTE_TOKEN_MINT` (this is an infrastructure config — the mint address of the quote token)
       - `TOKEN_DECIMALS`
       - `IPFS_GATEWAY`, `MAX_IMAGE_SIZE_MB`, `METADATA_MUTABLE`
       - `LAUNCHPAD_URL`
       - `FEE_UPDATE_INTERVAL_*` (cron intervals)
       - `ENABLE_FEES_DISPLAY`
    3. If any `process.env.NEXT_PUBLIC_*` variables for the removed defaults still exist, remove their parse lines. The `.env.local.example` file does NOT need to be modified in this phase.
    4. Verify the file still exports `ENV` and `SERVER_ENV` and the `Environment` type.
    5. Run `npx tsc --noEmit` to verify no TypeScript errors in this file.
  </action>
  <acceptance_criteria>
    - config/environment.ts does NOT contain `TOTAL_SUPPLY`, `INITIAL_PRICE`, `PRICE_RANGE_MIN`, `PRICE_RANGE_MAX`, `FEE_DECAY_DURATION_MINUTES`, `FEE_DECAY_PERIODS`, or `POOL_LIQUIDITY_PERCENTAGE`
    - config/environment.ts still contains `RPC_URL`, `SOLANA_NETWORK`, `QUOTE_TOKEN_MINT`, `TOKEN_DECIMALS`, `IPFS_GATEWAY`, `MAX_IMAGE_SIZE_MB`, `METADATA_MUTABLE`, `LAUNCHPAD_URL`, `FEE_UPDATE_INTERVAL_0_1H`, `FEE_UPDATE_INTERVAL_1_24H`, `FEE_UPDATE_INTERVAL_1_4D`, `FEE_UPDATE_INTERVAL_4D_PLUS`, `ENABLE_FEES_DISPLAY`
    - File still exports `ENV`, `SERVER_ENV`, and `Environment` type
    - `npx tsc --noEmit` reports 0 errors in config/environment.ts
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -c "config/environment.ts" || echo "0 errors in config/environment.ts"</automated>
  </verify>
  <done>Business defaults removed from environment.ts, only infrastructure config remains</done>
</task>

<task type="auto">
  <name>Task 2: Update lib/db/service.ts TokenCreateInput and createToken</name>
  <read_first>
    - lib/db/service.ts (current TokenCreateInput interface and createToken function)
    - lib/db/schema/tokens.ts (from Plan 03 — new column definitions)
    - 01-CONTEXT.md (D-02, D-13 for retroactive defaults and new column requirements)
  </read_first>
  <files>lib/db/service.ts</files>
  <action>
    Modify `lib/db/service.ts` to extend `TokenCreateInput` with all new fields from the schema. The interface is manually defined (not inferred from schema), so it must be updated to match:
    1. Add to `TokenCreateInput` interface (after the existing `poolLiquidityPercentage` field, before `feeDecayDurationMinutes`):
       - `priceRangeMin: number`
       - `priceRangeMax: number`
    2. Add after `feeDecayPeriods` (before `launchDate`):
       - `feeSchedulerMode: string`
       - `feeTokenMode: string`
       - `startingMarketCap: string`
       - `endingMarketCap: string`
       - `startRate: number`
       - `endRate: number`
       - `durationMinutes: number`
       - `fixedBaseFeeBps: number`
    3. Add after the fee fields (before `launchDate`):
       - `holdbackPercentage: number`
    4. In `createToken()`, the spread `...data` will automatically include all new fields when callers pass them. No changes needed to the insert logic since Drizzle ORM will use schema defaults for any missing fields.
    5. Import `DEFAULT_LAUNCH_PARAMS` from `@/config/defaults` if needed for any fallback logic (but createToken doesn't need defaults — callers should provide values).
    6. Ensure JSDoc comments are added for the new fields following existing conventions.
    7. Run `npx tsc --noEmit` to check for TypeScript errors.
  </action>
  <acceptance_criteria>
    - `TokenCreateInput` interface contains `priceRangeMin: number`
    - `TokenCreateInput` interface contains `priceRangeMax: number`
    - `TokenCreateInput` interface contains `feeSchedulerMode: string`
    - `TokenCreateInput` interface contains `feeTokenMode: string`
    - `TokenCreateInput` interface contains `startingMarketCap: string`
    - `TokenCreateInput` interface contains `endingMarketCap: string`
    - `TokenCreateInput` interface contains `startRate: number`
    - `TokenCreateInput` interface contains `endRate: number`
    - `TokenCreateInput` interface contains `durationMinutes: number`
    - `TokenCreateInput` interface contains `fixedBaseFeeBps: number`
    - `TokenCreateInput` interface contains `holdbackPercentage: number`
    - `createToken` function signature remains unchanged (it accepts `TokenCreateInput`)
    - `npx tsc --noEmit` reports 0 errors in lib/db/service.ts
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -c "lib/db/service.ts" || echo "0 errors in lib/db/service.ts"</automated>
  </verify>
  <done>TokenCreateInput extended with all new launch parameter fields</done>
</task>

<task type="auto">
  <name>Task 3: Update app/api/tokens/create/route.ts to accept new fields</name>
  <read_first>
    - app/api/tokens/create/route.ts (current request body parsing)
    - lib/db/service.ts (updated TokenCreateInput from Task 2)
    - config/defaults.ts (default values for fallbacks)
  </read_first>
  <files>app/api/tokens/create/route.ts</files>
  <action>
    Modify `app/api/tokens/create/route.ts` to accept and pass all new fields:
    1. Import `DEFAULT_LAUNCH_PARAMS` from `@/config/defaults` for fallback values.
    2. In the `tokenData: TokenCreateInput` object construction, add the new fields with fallback to defaults:
       - `priceRangeMin: data.priceRangeMin ?? DEFAULT_LAUNCH_PARAMS.priceRangeMin`
       - `priceRangeMax: data.priceRangeMax ?? DEFAULT_LAUNCH_PARAMS.priceRangeMax`
       - `feeSchedulerMode: data.feeSchedulerMode ?? DEFAULT_LAUNCH_PARAMS.feeSchedulerMode`
       - `feeTokenMode: data.feeTokenMode ?? DEFAULT_LAUNCH_PARAMS.feeTokenMode`
       - `startingMarketCap: data.startingMarketCap ?? DEFAULT_LAUNCH_PARAMS.startingMarketCap?.toString() ?? '0'`
       - `endingMarketCap: data.endingMarketCap ?? DEFAULT_LAUNCH_PARAMS.endingMarketCap?.toString() ?? '0'`
       - `startRate: data.startRate ?? DEFAULT_LAUNCH_PARAMS.startRate`
       - `endRate: data.endRate ?? DEFAULT_LAUNCH_PARAMS.endRate`
       - `durationMinutes: data.durationMinutes ?? DEFAULT_LAUNCH_PARAMS.durationMinutes`
       - `fixedBaseFeeBps: data.fixedBaseFeeBps ?? DEFAULT_LAUNCH_PARAMS.fixedBaseFeeBps`
       - `holdbackPercentage: data.holdbackPercentage ?? DEFAULT_LAUNCH_PARAMS.holdbackPercentage`
    3. Keep existing fields (including old `feeDecayDurationMinutes` and `feeDecayPeriods`) for backward compatibility during the transition.
    4. The request body validation can remain loose (using `??` fallbacks) since the database schema has NOT NULL defaults. Server-side strict validation will be added in Phase 3 (VALID-02).
    5. Run `npx tsc --noEmit` to verify compilation.
  </action>
  <acceptance_criteria>
    - route.ts imports `DEFAULT_LAUNCH_PARAMS` from `@/config/defaults`
    - `tokenData` object contains `priceRangeMin` with fallback to default
    - `tokenData` object contains `priceRangeMax` with fallback to default
    - `tokenData` object contains `feeSchedulerMode` with fallback to default
    - `tokenData` object contains `feeTokenMode` with fallback to default
    - `tokenData` object contains `startingMarketCap` with fallback to default
    - `tokenData` object contains `endingMarketCap` with fallback to default
    - `tokenData` object contains `startRate` with fallback to default
    - `tokenData` object contains `endRate` with fallback to default
    - `tokenData` object contains `durationMinutes` with fallback to default
    - `tokenData` object contains `fixedBaseFeeBps` with fallback to default
    - `tokenData` object contains `holdbackPercentage` with fallback to default
    - `npx tsc --noEmit` reports 0 errors in app/api/tokens/create/route.ts
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -c "app/api/tokens/create/route.ts" || echo "0 errors in route.ts"</automated>
  </verify>
  <done>API route accepts all new launch parameters with sensible defaults</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API route → Database | Untrusted client input crosses into persisted storage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-08 | Tampering | API route accepting arbitrary fee scheduler mode strings | mitigate | TypeScript types constrain the shape, but runtime validation is deferred to Phase 3 (VALID-02). The database stores the string value; invalid modes will fail at SDK call time in Phase 4/5. |
| T-01-09 | Information Disclosure | Default values in source code | accept | All defaults are public-facing form defaults, not secrets. No PII or credentials exposed. |
| T-01-10 | Denial of Service | Large request bodies with many fields | accept | Next.js body parsing limits apply. No unbounded array fields added. |
</threat_model>

<verification>
- `npx tsc --noEmit` passes for all three modified files
- No `TOTAL_SUPPLY`, `INITIAL_PRICE`, etc. remain in environment.ts
- TokenCreateInput includes all 11 new fields
- API route constructs tokenData with all new fields and fallback defaults
</verification>

<success_criteria>
- config/environment.ts contains only infrastructure config (no business defaults)
- lib/db/service.ts TokenCreateInput has all new fields
- app/api/tokens/create/route.ts accepts and forwards all new fields with fallbacks
- TypeScript compilation is clean for all three files
</success_criteria>

<output>
Create `.planning/phases/01-types-schema-defaults-foundation/01-04-SUMMARY.md` when done
</output>
