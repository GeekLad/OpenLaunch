---
phase: 01-types-schema-defaults-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - lib/solana/poolUtils.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "@meteora-ag/cp-amm-sdk is installed at version 1.4.3"
    - "poolUtils.ts compiles without TypeScript errors after SDK upgrade"
    - "All breaking SDK API changes from v1.2.3 to v1.4.3 are addressed in poolUtils.ts"
  artifacts:
    - path: "package.json"
      provides: "SDK version constraint ^1.4.3"
      contains: '"@meteora-ag/cp-amm-sdk": "^1.4.3"'
    - path: "lib/solana/poolUtils.ts"
      provides: "Updated pool creation with v1.4.3 API"
      contains: "getFeeTimeSchedulerParams"
      contains: "getFeeMarketCapSchedulerParams"
      contains: "FeeTimeSchedulerExponential"
      contains: "compoundingFeeBps"
  key_links:
    - from: "lib/solana/poolUtils.ts"
      to: "@meteora-ag/cp-amm-sdk v1.4.3"
      via: "import statements"
      pattern: "from \"@meteora-ag/cp-amm-sdk\""
---

<objective>
Upgrade the Meteora DAMMv2 SDK from v1.2.3 to v1.4.3 and update `lib/solana/poolUtils.ts` to use the new API. This unblocks market-cap-based fee scheduler support required by D-14 and prevents runtime failures from breaking changes.

Purpose: The SDK upgrade is a hard requirement (D-14). v1.4.3 introduces new fee scheduler constructors, renames enum values, and changes `PoolFeesParams` shape. Without this upgrade, Phase 4/5 blockchain integration will fail.
Output: package.json with ^1.4.3 constraint, poolUtils.ts with corrected imports and API usage.
</objective>

<execution_context>
@/home/geeklad/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/01-types-schema-defaults-foundation/01-CONTEXT.md
@.planning/phases/01-types-schema-defaults-foundation/01-RESEARCH.md
@.planning/phases/01-types-schema-defaults-foundation/01-PATTERNS.md
@lib/solana/poolUtils.ts
@package.json
</context>

<interfaces>
From lib/solana/poolUtils.ts (current v1.2.3 API usage):
- Imports: `CpAmm`, `PoolFeesParams`, `bpsToFeeNumerator`, `getFeeSchedulerParams`, `BaseFeeMode`, `CollectFeeMode`, `getDynamicFeeParams`
- Old enum: `BaseFeeMode.FeeSchedulerExponential`
- Old poolFees shape: `padding: [0, 0, 0]` (number[])
- Old function: `getFeeSchedulerParams(startBps, endBps, BaseFeeMode.FeeSchedulerExponential, numberOfPeriods, durationSeconds)`
- CollectFeeMode: `CollectFeeMode.OnlyB`

From 01-RESEARCH.md (v1.4.3 changes):
- `getFeeSchedulerParams` → `getFeeTimeSchedulerParams`
- `BaseFeeMode.FeeSchedulerExponential` → `BaseFeeMode.FeeTimeSchedulerExponential`
- New: `getFeeMarketCapSchedulerParams(startBps, endBps, BaseFeeMode.FeeMarketCapSchedulerExponential, numberOfPeriods, priceMultiple, schedulerExpirationDuration)`
- `PoolFeesParams.padding` from `number[]` to `number`
- New field: `compoundingFeeBps: 0`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade @meteora-ag/cp-amm-sdk to v1.4.3</name>
  <read_first>
    - package.json (current dependency versions)
    - 01-RESEARCH.md (SDK breaking changes section)
  </read_first>
  <files>package.json</files>
  <action>
    Update the `@meteora-ag/cp-amm-sdk` dependency in package.json from `"^1.2.3"` to `"^1.4.3"` (per D-14). Run `npm install` to update the installed version. Verify the installed version by checking `node_modules/@meteora-ag/cp-amm-sdk/package.json`.
  </action>
  <acceptance_criteria>
    - package.json contains `"@meteora-ag/cp-amm-sdk": "^1.4.3"`
    - `npm install` completes without errors
    - `node_modules/@meteora-ag/cp-amm-sdk/package.json` shows version `1.4.3`
  </acceptance_criteria>
  <verify>
    <automated>cat node_modules/@meteora-ag/cp-amm-sdk/package.json | grep '"version"'</automated>
  </verify>
  <done>SDK v1.4.3 is installed and ready for use</done>
</task>

<task type="auto">
  <name>Task 2: Update poolUtils.ts for v1.4.3 API</name>
  <read_first>
    - lib/solana/poolUtils.ts (current implementation)
    - 01-RESEARCH.md (Common Pitfalls section: Pitfall 1, 2, 4)
    - 01-PATTERNS.md (poolUtils.ts replacement pattern)
  </read_first>
  <files>lib/solana/poolUtils.ts</files>
  <action>
    Update lib/solana/poolUtils.ts to use the v1.4.3 SDK API. Make these specific changes per RESEARCH.md:
    1. Update imports: replace `getFeeSchedulerParams` with `getFeeTimeSchedulerParams`; add `getFeeMarketCapSchedulerParams` import
    2. Replace `BaseFeeMode.FeeSchedulerExponential` with `BaseFeeMode.FeeTimeSchedulerExponential`
    3. Update `PoolFeesParams.padding` from `number[]` (`[0, 0, 0]`) to `number` (`0`) in both the fee-scheduler branch and the fixed-fee branch
    4. Add `compoundingFeeBps: 0` to all `poolFees` object constructions
    5. Keep `CollectFeeMode.OnlyB` for now (both-token mode is Phase 6 scope)
    6. Do NOT implement market-cap scheduler logic yet — just ensure imports and type shapes are correct. The actual market-cap scheduler usage will be built in Phase 4/5.
    
    After editing, run `npx tsc --noEmit` or `npm run type-check` to verify compilation.
  </action>
  <acceptance_criteria>
    - lib/solana/poolUtils.ts imports `getFeeTimeSchedulerParams` and `getFeeMarketCapSchedulerParams` from `@meteora-ag/cp-amm-sdk`
    - `BaseFeeMode.FeeSchedulerExponential` is completely removed from the file
    - `BaseFeeMode.FeeTimeSchedulerExponential` appears in the file
    - All `poolFees` objects contain `compoundingFeeBps: 0`
    - All `poolFees` objects use `padding: 0` (not an array)
    - `npm run type-check` (or `npx tsc --noEmit`) passes without errors related to poolUtils.ts
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -c poolUtils.ts || echo "0 poolUtils.ts errors"</automated>
  </verify>
  <done>poolUtils.ts compiles cleanly with SDK v1.4.3</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local node_modules | External package crosses into build artifacts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | npm install of @meteora-ag/cp-amm-sdk | mitigate | Package legitimacy verified in RESEARCH.md (official MeteoraAg GitHub org, 133 versions). Install from npm registry with exact version `1.4.3`. |
| T-01-02 | Information Disclosure | package.json version bump | accept | Version string is public information; no sensitive data in version constraint. |
| T-01-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck unavailable per RESEARCH.md. Package verified via npm registry + official GitHub org linkage. Treat as [ASSUMED] legitimate. |
</threat_model>

<verification>
- `npm run type-check` passes (or `npx tsc --noEmit` as fallback)
- `node_modules/@meteora-ag/cp-amm-sdk/package.json` shows version 1.4.3
- No `BaseFeeMode.FeeSchedulerExponential` references remain in poolUtils.ts
</verification>

<success_criteria>
- SDK v1.4.3 is installed
- poolUtils.ts uses correct v1.4.3 imports and type shapes
- TypeScript compilation passes for poolUtils.ts
</success_criteria>

<output>
Create `.planning/phases/01-types-schema-defaults-foundation/01-01-SUMMARY.md` when done
</output>
