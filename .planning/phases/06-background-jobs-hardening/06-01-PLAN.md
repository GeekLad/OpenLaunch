---
phase: 06-background-jobs-hardening
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - vitest.config.ts
  - tests/fixtures/meteora-pool-response.json
  - tests/cron/fee-updater.test.ts
  - tests/meteora/client.test.ts
  - tests/db/fee-update-schedule.test.ts
  - lib/db/schema/fee-update-schedule.ts
  - lib/db/migrations/0007_*.sql
autonomous: true
requirements: [CRON-01, CRON-02, CRON-03]
tags: [cron, meteora, schema, testing]

must_haves:
  truths:
    - "Running the test command executes test files without framework errors"
    - "Tests assert the FIXED behavior (USD microunits, new interface, circuit breaker, stale filter) and currently FAIL because the code is not yet fixed"
    - "The fee_update_schedule table has a `stale` boolean column defaulting to false after the schema push"
  artifacts:
    - path: "vitest.config.ts"
      provides: "Test runner config aliasing @/* and using node environment"
      min_lines: 8
    - path: "tests/cron/fee-updater.test.ts"
      provides: "Failing tests asserting USD microunits conversion, feeTokenMode read, circuit breaker threshold, no *1e9 regression"
      min_lines: 40
    - path: "tests/meteora/client.test.ts"
      provides: "Failing tests asserting new MeteoraPoolMetrics shape parse and new base URLs"
      min_lines: 25
    - path: "tests/db/fee-update-schedule.test.ts"
      provides: "Failing tests asserting getPoolsDueForUpdate excludes stale and markPoolStale sets stale=true"
      min_lines: 25
    - path: "lib/db/schema/fee-update-schedule.ts"
      provides: "stale boolean column on fee_update_schedule"
      contains: "stale: integer('stale', { mode: 'boolean' })"
    - path: "lib/db/migrations/0007_*.sql"
      provides: "Generated ALTER TABLE adding stale column"
      contains: "ADD COLUMN stale"
  key_links:
    - from: "lib/db/schema/fee-update-schedule.ts"
      to: "lib/db/migrations/0007_*.sql"
      via: "drizzle-kit generate from schema diff"
      pattern: "stale"
---

<objective>
Bootstrap the test harness with FAILING tests that encode Phase 6's fixed behavior, and land the `stale` schema column with a generated migration applied to the live SQLite database.

Purpose: Wave 0 establishes the verification scaffold (tests go RED now, GREEN in Waves 1-2) and the database prerequisite (`stale` column) that `lib/db/service.ts` and `lib/cron/fee-updater.ts` will reference in later waves. Per Pitfall 6, the migration MUST be generated AND applied before any code references `stale`, otherwise SQLite throws `no such column: stale`.

Output: `vitest.config.ts`, three failing test files, a captured live API fixture, the `stale` column in `fee-update-schedule.ts`, a generated `0007_*.sql` migration, and the schema pushed to the live DB.
</objective>

<execution_context>
@/home/geeklad/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/geeklad/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-background-jobs-hardening/06-CONTEXT.md
@.planning/phases/06-background-jobs-hardening/06-RESEARCH.md
@.planning/phases/06-background-jobs-hardening/06-PATTERNS.md
@.planning/phases/06-background-jobs-hardening/06-VALIDATION.md

@lib/db/schema/fee-update-schedule.ts
@lib/db/schema/tokens.ts
@lib/db/migrations/0006_cleanup_obsolete_columns.sql
@lib/cron/fee-updater.ts
@lib/meteora/client.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bootstrap test harness with failing tests encoding fixed behavior</name>
  <files>package.json, vitest.config.ts, tests/fixtures/meteora-pool-response.json, tests/cron/fee-updater.test.ts, tests/meteora/client.test.ts, tests/db/fee-update-schedule.test.ts</files>
  <read_first>
    - package.json (current scripts block — add `test`/`test:run`; confirm no test framework present)
    - lib/cron/fee-updater.ts (current buggy conversion at lines 47-69, failure handling at 88-113 — tests assert the FIXED behavior these become)
    - lib/meteora/client.ts (current buggy interface lines 6-39, URL lines 44-53 — tests assert the NEW shape)
    - lib/db/service.ts (lines 442-481: getPoolsDueForUpdate, recordUpdateFailure, getFeeUpdateSchedule — tests assert stale-filtered query + markPoolStale)
    - lib/db/schema/fee-update-schedule.ts (current schema — tests reference FeeUpdateSchedule.stale)
    - tsconfig.json (path alias `@/*` → must mirror in vitest config)
    - config/defaults.ts (DEFAULT_FEE_TOKEN_MODE — fixture/expectation source)
  </read_first>
  <behavior>
    - Cron test: feeding a fixture with `cumulative_metrics.fees = 277672.23` asserts `updateCumulativeFeesSnapshot` is called with exactly `"277672230000"` (Math.floor(277672.23 * 1e6))
    - Cron test: feeding `fees.24h = 4.424238` asserts `fees24h` argument = `"4424238"`
    - Cron test: a separate assertion checks the source file `lib/cron/fee-updater.ts` contains no literal `* 1e9` (regression guard) — this PASSES only after Wave 2, so it is RED now
    - Cron test: mock token with `feeTokenMode: 'bothTokens'` asserts a deferred-per-side log is emitted; `feeTokenMode: 'quoteOnly'` asserts no such warning
    - Cron test: after recordUpdateFailure, mock getFeeUpdateSchedule returning `consecutiveFailures: 10` asserts `markPoolStale` called; returning 9 asserts NOT called
    - Client test: a fixture matching the verified live Meteora response parses into `MeteoraPoolMetrics` with `cumulative_metrics.fees`, `fees["24h"]`, `pool_config.collect_fee_mode`, `farm_apr`
    - Client test: `getMeteoraApiBaseUrl()` for mainnet returns `https://damm-v2.datapi.meteora.ag`; for devnet returns `https://damm-v2-api.dev.metdev.io`
    - DB test: `getPoolsDueForUpdate` query includes `eq(feeUpdateSchedule.stale, false)` filter (assert via mocked query builder or in-memory sqlite)
    - DB test: `markPoolStale(tokenId)` issues `db.update(feeUpdateSchedule).set({ stale: true }).where(eq(..., tokenId))`
  </behavior>
  <action>
    Add a `test` script to package.json: `"test": "vitest run"` and `"test:watch": "vitest"`. Install `vitest` as a devDependency (RESEARCH Section "Validation Architecture" recommends vitest; run `npm install -D vitest @vitest/expect`). Before installing, verify package legitimacy: `npm view vitest` confirms it is published by the vitest-dev org, has no postinstall script, and is widely downloaded. Tag the install as [VERIFIED: npm registry] in the commit body. If slopcheck is available, run `slopcheck install vitest --json` and expect `[OK]`; if not available, treat as [ASSUMED] and note in the commit body. Per the security gate, package legitimacy checkpoints for new runtime packages are never auto-approvable — however vitest is a DEV/test-only dependency (never shipped to production or imported by runtime code), so it is low-risk; document this rationale in the threat model (Plan 01 owns T-06-SC).

    Create `vitest.config.ts` importing `defineConfig` from `vitest/config`, setting `test.environment` to `"node"`, `test.include` to `["tests/**/*.test.ts"]`, and `resolve.alias` mapping `@` to the project root (mirroring tsconfig `paths` `@/*`). Set `test.globals` to true and `test.restoreMocks` to true so each test resets `global.fetch` and `dbService` spies.

    Create `tests/fixtures/meteora-pool-response.json` containing a captured-shape fixture. Do NOT require live network at test time — hand-write a fixture matching the verified live response shape from RESEARCH.md "Code Examples > New MeteoraPoolMetrics Interface": include `address`, `name`, `token_x`/`token_y` (TokenMetrics with decimals/price/market_cap), `tvl` (e.g. 277672.23), `current_price`, `volume` (TimeWindowData with 30m/1h/2h/4h/12h/24h keys, set `24h` to e.g. 12345.67), `fees` (TimeWindowData, set `24h` to 4.424238), `protocol_fees`, `fee_tvl_ratio`, `cumulative_metrics` (`{ volume: 999999.5, fees: 277672.23 }`), `pool_config` (`{ collect_fee_mode: 1, base_fee_mode: 0, base_fee_pct: 25, ... }`), `farm_apr` (e.g. 45.2), `has_farm: false`, `is_blacklisted: false`. Also add a second fixture variant `meteora-pool-response-both-mode.json` identical but with `pool_config.collect_fee_mode: 0` for the both-token equivalence test.

    Create `tests/cron/fee-updater.test.ts` using `vitest`'s `describe/it/expect` and `vi.mock`. Mock `@/lib/meteora/client` (`getPoolMetrics` returns the fixture), mock `@/lib/db/service` (spy on `updateCumulativeFeesSnapshot`, `createPoolStatsSnapshot`, `upsertFeeUpdateSchedule`, `recordUpdateFailure`, `getFeeUpdateSchedule`, `markPoolStale`, `getTokenById`, `getPoolsDueForUpdate`). Import `updateTokenFees` from `@/lib/cron/fee-updater`. Write tests for each `<behavior>` item above. Tests MUST FAIL now (code is not fixed) — this is the RED phase. Read the source first to ensure mock shapes match real signatures (dbService is imported as `* as dbService` namespace, so mock with `vi.mock("@/lib/db/service", () => ({ getTokenById: vi.fn(), ... }))` and assert via `expect(dbService.updateCumulativeFeesSnapshot).toHaveBeenCalledWith(mintAddress, "277672230000")`).

    Create `tests/meteora/client.test.ts` mocking `global.fetch` via `vi.stubGlobal("fetch", vi.fn())`. Test that a 200 response whose `.json()` resolves to the fixture yields a `MeteoraPoolMetrics` with `cumulative_metrics.fees === 277672.23` and `fees["24h"] === 4.424238`. Test 404 returns null. Test `getMeteoraApiBaseUrl` by setting `SOLANA_NETWORK` (mock `@/config/public`) to mainnet vs devnet and asserting the URLs in RESEARCH D-12. Tests FAIL now because the interface/URL still hold the buggy values.

    Create `tests/db/fee-update-schedule.test.ts`. Use an in-memory `better-sqlite3` database (`new Database(":memory:")`) wired to a drizzle instance, OR mock the `db` export from `@/lib/db/client`. Simplest compliant path: mock `@/lib/db/client`'s `db` with a spy object whose `.select()/.from()/.where()/.orderBy()/.limit()` chain returns controlled rows, and `.update()/.set()/.where()` records the call. Assert `getPoolsDueForUpdate` result excludes rows where stale=true, and `markPoolStale(5)` calls `db.update(feeUpdateSchedule).set({ stale: true }).where(eq(feeUpdateSchedule.tokenId, 5))`. Tests FAIL now (markPoolStale does not exist; getPoolsDueForUpdate does not filter stale).

    Do NOT edit any production logic in this task — only test files, config, package.json, and fixtures. The tests are intentionally RED.
  </read_first>
  <verify>
    <automated>npm install --silent && npx vitest run --reporter=dot 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains `"test": "vitest run"` in scripts
    - vitest.config.ts exists and sets `environment: "node"` and an `@` path alias
    - `node -e "require('vitest')"` succeeds (vitest installed)
    - tests/cron/fee-updater.test.ts contains the string `"277672230000"` (the USD-microunits boundary assertion)
    - tests/cron/fee-updater.test.ts contains a test asserting the source file has no `* 1e9` literal
    - tests/meteora/client.test.ts contains `damm-v2.datapi.meteora.ag` and `damm-v2-api.dev.metdev.io`
    - tests/db/fee-update-schedule.test.ts references `markPoolStale` and `stale`
    - `npx vitest run` exits NON-ZERO (tests fail, as expected for the RED phase) — this is the success condition for this task; document the failure count in the SUMMARY
    - No production file (lib/cron, lib/meteora, lib/db/service.ts, lib/db/schema) is modified in this task
  </acceptance_criteria>
  <done>vitest runs and reports failing tests for all CRON-01/02/03 behaviors plus the client/DB fixes; the RED phase is established. Package legitimacy for vitest is documented (VERIFIED or ASSUMED with rationale) in the commit body.</done>
</task>

<task type="auto">
  <name>Task 2: Add `stale` column, generate migration, and BLOCKING schema push</name>
  <files>lib/db/schema/fee-update-schedule.ts, lib/db/migrations/0007_*.sql</files>
  <read_first>
    - lib/db/schema/fee-update-schedule.ts (full file — add `stale` after `lastErrorAt` at line 35)
    - lib/db/schema/tokens.ts (line 27 — the `featured: integer('featured', { mode: 'boolean' }).notNull().default(false)` pattern to copy EXACTLY)
    - lib/db/migrations/0006_cleanup_obsolete_columns.sql (drizzle-kit output format — the generated 0007 file should match this style)
    - drizzle.config.ts (confirms schema source and migrations folder)
    - package.json (`db:generate` and `db:migrate` scripts — use db:generate here, then drizzle-kit push per the BLOCKING requirement)
  </read_first>
  <action>
    Edit `lib/db/schema/fee-update-schedule.ts`: add a new `stale` column immediately after the `lastErrorAt` column (line 35), inside the `sqliteTable('fee_update_schedule', {...})` object, before the closing of the columns object and the indexes callback. Use the EXACT pattern from `lib/db/schema/tokens.ts:27` (`featured`): `stale: integer('stale', { mode: 'boolean' }).notNull().default(false),`. Add an inline comment above it: `// Circuit breaker flag — when true, cron skips this pool entirely (D-14/D-15)`. Do NOT add a new index (D-15 filters via the WHERE clause; the existing `nextUpdateIdx` is sufficient for the due-update query). The `FeeUpdateSchedule` and `NewFeeUpdateSchedule` types at the bottom auto-update — do NOT edit them manually.

    Run `npm run db:generate` (drizzle-kit generate). This produces `lib/db/migrations/0007_*.sql` containing an `ALTER TABLE fee_update_schedule ADD COLUMN stale INTEGER NOT NULL DEFAULT 0;` statement (drizzle emits boolean as INTEGER 0/1 for SQLite). Confirm the file was created with the expected SQL.

    Then run the BLOCKING schema push per the orchestrator's schema_push_requirement: `npx drizzle-kit push`. drizzle-kit push is non-interactive for SQLite by default. If it prompts interactively (cannot be suppressed), fall back to `npm run db:migrate` (which runs `tsx lib/db/migrate.ts`) to apply the generated 0007 migration instead — and flag `autonomous: false` would have been needed, but prefer the migrate path to stay autonomous. After push/migrate, verify the live SQLite database actually has the column.

    The push is BLOCKING — the phase cannot pass verification without the live DB having the `stale` column, because Waves 1-2 code will reference it and runtime errors (`no such column: stale`) would otherwise occur (Pitfall 6).
  </read_first>
  <verify>
    <automated>npm run db:generate && npx drizzle-kit push && node -e "const Database = require('better-sqlite3'); const db = new Database(process.env.DATA_DIR ? process.env.DATA_DIR + '/db.sqlite' : './data/db.sqlite'); const row = db.prepare(\"PRAGMA table_info(fee_update_schedule)\").all().find(c => c.name === 'stale'); if (!row) { console.error('stale column MISSING'); process.exit(1); } console.log('stale column OK:', row);"</automated>
  </verify>
  <acceptance_criteria>
    - lib/db/schema/fee-update-schedule.ts contains the literal `stale: integer('stale', { mode: 'boolean' }).notNull().default(false)`
    - `grep -c "1e9" lib/db/schema/fee-update-schedule.ts` returns 0 (no accidental unit bug introduced)
    - lib/db/migrations/ contains a new file matching `0007_*.sql`
    - The 0007 migration file contains `ADD COLUMN stale` (case may vary) — confirm via grep
    - `PRAGMA table_info(fee_update_schedule)` against the live SQLite DB returns a row with `name === 'stale'` and `dflt_value` of `0` or `false`
    - `npm run type-check` (tsc --noEmit) passes — the `stale` field is now on the `FeeUpdateSchedule` inferred type
  </acceptance_criteria>
  <done>The `stale` boolean column exists in the schema, a generated 0007 migration is committed, the live SQLite database has the column, and TypeScript type-checks pass. Downstream waves can now reference `feeUpdateSchedule.stale` without runtime errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev dependency → build | vitest is installed as a devDependency; it must not be imported by any runtime code under `lib/`, `app/`, `components/`, `config/` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-SC | Tampering / Supply Chain | npm install of vitest | mitigate | vitest is dev/test-only (never imported by runtime code). Verify via `npm view vitest` (publisher vitest-dev, no postinstall script). Tag [VERIFIED: npm registry] or [ASSUMED] in commit. If `slopcheck` is available, run `slopcheck install vitest --json` expecting [OK]. Add an ESLint/CI guard later if desired (out of scope). Low runtime risk because vitest never ships to production (Next.js standalone build excludes devDependencies). |
| T-06-MIG | Tampering | drizzle-kit push altering live DB | accept | Push is idempotent ADD COLUMN with default; existing rows get `stale=false`. No data loss. Migration is generated (reviewable SQL) not hand-written. |
</threat_model>

<verification>
- `npx vitest run` reports failing tests (RED phase established) — failing is the EXPECTED success state for Task 1
- `npm run type-check` passes after the `stale` column is added
- Live SQLite DB has the `stale` column (Task 2 automated verify)
</verification>

<success_criteria>
- Test harness runs and reports RED tests encoding all CRON-01/02/03 + client/DB fixed behaviors
- `stale` column exists in schema, migration generated, live DB updated
- No production logic changed yet (only schema + tests)
</success_criteria>

<output>
Create `.planning/phases/06-background-jobs-hardening/06-01-SUMMARY.md` when done
</output>