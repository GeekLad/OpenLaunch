---
phase: 06-background-jobs-hardening
plan: 03
type: execute
wave: 2
depends_on: ["06-02"]
files_modified:
  - lib/cron/fee-updater.ts
  - config/defaults.ts
autonomous: true
requirements: [CRON-01, CRON-02, CRON-03]
tags: [cron, fee-updater, circuit-breaker]

must_haves:
  truths:
    - "Cron reads token.feeTokenMode from the DB for each pool and logs it (CRON-01)"
    - "Cron converts all monetary metrics to USD microunits (Math.floor(usd * 1e6)) — no * 1e9 anywhere (CRON-02)"
    - "Quote-only and both-token pools follow the identical aggregate-USD store path; both-token pools emit a deferred-per-side log warning (CRON-03)"
    - "Cron uses cumulative_metrics.fees (not the non-existent lp_fee30d) for cumulativeFeesSnapshot"
    - "After MAX_CONSECUTIVE_FAILURES (default 10), the cron calls markPoolStale and logs a warning"
    - "Historical pre-Phase-6 snapshots remain untouched (no backfill migration — D-20)"
  artifacts:
    - path: "lib/cron/fee-updater.ts"
      provides: "Rewritten updateTokenFees with USD microunits, feeTokenMode read, circuit breaker threshold check"
      contains: "MAX_CONSECUTIVE_FAILURES"
    - path: "config/defaults.ts"
      provides: "MAX_CONSECUTIVE_FAILURES constant (default 10)"
      contains: "MAX_CONSECUTIVE_FAILURES"
  key_links:
    - from: "lib/cron/fee-updater.ts"
      to: "lib/meteora/client.ts"
      via: "getPoolMetrics import"
      pattern: "getPoolMetrics"
    - from: "lib/cron/fee-updater.ts"
      to: "lib/db/service.ts"
      via: "dbService.markPoolStale + getFeeUpdateSchedule"
      pattern: "markPoolStale"
    - from: "lib/cron/fee-updater.ts"
      to: "config/defaults.ts"
      via: "MAX_CONSECUTIVE_FAILURES import"
      pattern: "MAX_CONSECUTIVE_FAILURES"
---

<objective>
Rewrite `updateTokenFees()` in the cron to: (1) read `token.feeTokenMode` per pool and log it (CRON-01), (2) convert all monetary metrics to USD microunits using `cumulative_metrics.fees` / `fees["24h"]` / `volume["24h"]` / `tvl` / `farm_apr` with `* 1e6` and `?? 0` guards (CRON-02), (3) treat both-token and quote-only pools identically with a deferred-per-side log warning for both-token pools (CRON-03), and (4) trigger the circuit breaker via `markPoolStale` when `consecutiveFailures >= MAX_CONSECUTIVE_FAILURES` after re-fetching the schedule row.

Purpose: This is the capstone task — it consumes the fixed client (Plan 02) and the new DB primitives (Plan 02) to make the cron actually write correct, non-zero USD-microunits values and stop hammering dead pools. After this plan, the cron is no longer silently broken.

Output: Rewritten `lib/cron/fee-updater.ts` (conversion block, mode read, circuit breaker in both failure branches), `config/defaults.ts` gains `MAX_CONSECUTIVE_FAILURES`.
</objective>

<execution_context>
@/home/geeklad/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/geeklad/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-background-jobs-hardening/06-CONTEXT.md
@.planning/phases/06-background-jobs-hardening/06-RESEARCH.md
@.planning/phases/06-background-jobs-hardening/06-PATTERNS.md
@.planning/phases/06-background-jobs-hardening/06-01-SUMMARY.md
@.planning/phases/06-background-jobs-hardening/06-02-SUMMARY.md

@lib/cron/fee-updater.ts
@lib/db/service.ts
@lib/meteora/client.ts
@config/defaults.ts
@types/fee.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add MAX_CONSECUTIVE_FAILURES constant to config/defaults.ts</name>
  <files>config/defaults.ts</files>
  <read_first>
    - config/defaults.ts (full file — find the fee-token-mode block around line 17 to place the new constant nearby; confirm `DEFAULT_FEE_TOKEN_MODE` is exported there)
    - .planning/phases/06-background-jobs-hardening/06-CONTEXT.md D-17 (constant lives in config/defaults.ts or fee-updater.ts; RESEARCH Open Question #2 recommends config/defaults.ts for centralization)
  </read_first>
  <action>
    Per D-17 and RESEARCH Open Question #2 recommendation. Add a new exported constant `MAX_CONSECUTIVE_FAILURES` to `config/defaults.ts` near the existing fee-token-mode constants (around line 17, after `DEFAULT_FEE_TOKEN_MODE`). Value: `10` (the default threshold from D-14/D-17). Add an inline JSDoc/comment: `// Circuit breaker threshold — after this many consecutive cron failures, a pool is marked stale and skipped (D-14/D-17)`. Use `as const` to lock the literal type (matches the `DEFAULT_FEE_TOKEN_MODE = 'quoteOnly' as const` pattern at line 17). Do NOT change any existing constants.
  </read_first>
  <verify>
    <automated>node -e "const { MAX_CONSECUTIVE_FAILURES } = require('./config/defaults.ts'); if (MAX_CONSECUTIVE_FAILURES !== 10) { console.error('expected 10, got', MAX_CONSECUTIVE_FAILURES); process.exit(1); } console.log('OK', MAX_CONSECUTIVE_FAILURES)" 2>/dev/null || npx tsx -e "import { MAX_CONSECUTIVE_FAILURES } from './config/defaults'; if (MAX_CONSECUTIVE_FAILURES !== 10) process.exit(1); console.log('OK', MAX_CONSECUTIVE_FAILURES)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "MAX_CONSECUTIVE_FAILURES" config/defaults.ts` returns >=1 (export present)
    - The constant value is `10` (verified via the automated check)
    - The constant is exported (no private/local-only declaration)
    - `npm run type-check` passes
    - No existing constant in config/defaults.ts is modified
  </acceptance_criteria>
  <done>MAX_CONSECUTIVE_FAILURES = 10 is exported from config/defaults.ts and importable by the cron.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite cron updateTokenFees — USD microunits, feeTokenMode read, circuit breaker</name>
  <files>lib/cron/fee-updater.ts</files>
  <read_first>
    - lib/cron/fee-updater.ts (full file — the buggy conversion block lines 47-69, the two failure branches lines 88-99 and 100-113, the success log line 86; all are replaced)
    - tests/cron/fee-updater.test.ts (from Plan 01 — assertions define target behavior; make GREEN)
    - lib/meteora/client.ts (after Plan 02 — new MeteoraPoolMetrics shape: cumulative_metrics.fees, fees["24h"], volume["24h"], tvl, farm_apr, pool_config.collect_fee_mode)
    - lib/db/service.ts (after Plan 02 — dbService.markPoolStale, dbService.getFeeUpdateSchedule exist; getPoolsDueForUpdate now filters stale)
    - config/defaults.ts (after Task 1 — MAX_CONSECUTIVE_FAILURES exported)
    - .planning/phases/06-background-jobs-hardening/06-PATTERNS.md "lib/cron/fee-updater.ts (service, event-driven) — REWRITE" (exact replacement blocks)
    - .planning/phases/06-background-jobs-hardening/06-CONTEXT.md D-01, D-02, D-03, D-05, D-06, D-08, D-09, D-14, D-17, D-20, D-22
    - lib/db/schema/tokens.ts (line 50 — token.feeTokenMode field exists, type string default 'quoteOnly')
  </read_first>
  <behavior>
    - cron test: fixture cumulative_metrics.fees=277672.23 → updateCumulativeFeesSnapshot called with "277672230000"
    - cron test: fixture fees["24h"]=4.424238 → createPoolStatsSnapshot called with fees24h="4424238"
    - cron test: source file lib/cron/fee-updater.ts contains NO `* 1e9` literal (regression guard — grep returns 0)
    - cron test: token.feeTokenMode='bothTokens' → deferred-per-side log emitted; 'quoteOnly' → no such warning
    - cron test: both-token and quote-only fixtures with identical cumulative_metrics.fees produce IDENTICAL updateCumulativeFeesSnapshot calls (CRON-03 equivalence)
    - cron test: after recordUpdateFailure, getFeeUpdateSchedule returning consecutiveFailures=10 → markPoolStale called; =9 → NOT called
    - cron test: missing cumulative_metrics (undefined) → updateCumulativeFeesSnapshot called with "0" (defensive ?? 0, not "NaN")
  </behavior>
  <action>
    Per D-01/D-02/D-03/D-05/D-06/D-08/D-09/D-14/D-17/D-20/D-22 and PATTERNS.md "lib/cron/fee-updater.ts — REWRITE". Add an import at the top: `import { MAX_CONSECUTIVE_FAILURES } from "@/config/defaults";` (keep existing imports lines 1-4 unchanged: node-cron, getPoolMetrics, calculateNextUpdateTime, dbService).

    INSERT the CRON-01 mode read immediately after the `if (!token) { continue; }` guard (after line 43), before `const metrics = await getPoolMetrics(...)`:
    `if (token.feeTokenMode === 'bothTokens') { console.log(\`[Cron] Pool ${token.poolAddress} uses both-token fee mode; tracking aggregate USD fees (per-side tracking deferred per CRON-03)\`); }`. The fetch+store path is IDENTICAL regardless of mode (D-05/D-22) — do NOT branch on feeTokenMode for the store logic.

    REPLACE the buggy conversion block (lines 47-69) with the USD-microunits block per D-02/D-08/D-09: define `const USD_MICROUNITS = 1_000_000;` then `const cumulativeFeesMicro = Math.floor((metrics.cumulative_metrics?.fees ?? 0) * USD_MICROUNITS);` `const fees24hMicro = Math.floor(metrics.fees["24h"] * USD_MICROUNITS);` `const volume24hMicro = Math.floor(metrics.volume["24h"] * USD_MICROUNITS);` `const tvlMicro = Math.floor(metrics.tvl * USD_MICROUNITS);` `const apr = metrics.farm_apr ?? null;` (D-03 — APR is a percentage stored as-is as real/float; no multiplication). Call `dbService.updateCumulativeFeesSnapshot(token.mintAddress, cumulativeFeesMicro.toString())` and `dbService.createPoolStatsSnapshot(token.id, token.poolAddress, { totalFeesGenerated: cumulativeFeesMicro.toString(), fees24h: fees24hMicro.toString(), volume24h: volume24hMicro.toString(), currentLiquidity: tvlMicro.toString(), apr })`. All values passed to dbService MUST be strings (`.toString()`) per the text-precision storage convention, EXCEPT `apr` which is `number | null` (real column).

    UPDATE the success log (line 86) to: `console.log(\`[Cron] ✓ Updated fees for token ${token.id} (${token.symbol}): ${cumulativeFeesMicro} USD microunits (cumulative=${metrics.cumulative_metrics?.fees ?? 0} USD, mode=${token.feeTokenMode})\`)`.

    EXTEND the `metrics === null` failure branch (lines 88-99): keep the existing `dbService.recordUpdateFailure(token.id, "Pool not found in Meteora DAMMv2 API (404)")` call, then ADD the circuit breaker check per D-14 and Pitfall 4 (re-fetch to read post-increment count because recordUpdateFailure returns void — Assumption A4): `const updated = await dbService.getFeeUpdateSchedule(token.id); if (updated && updated.consecutiveFailures >= MAX_CONSECUTIVE_FAILURES) { await dbService.markPoolStale(token.id); console.warn(\`[Cron] ⚠️ Pool ${token.poolAddress} marked stale after ${MAX_CONSECUTIVE_FAILURES} consecutive failures\`); }`.

    EXTEND the `catch` failure branch (lines 100-113) IDENTICALLY: after `dbService.recordUpdateFailure(schedule.tokenId, errorMessage)`, add the same re-fetch + threshold + markPoolStale block (using `schedule.tokenId`). Use `schedule.poolAddress ?? token?.poolAddress ?? 'unknown'` in the stale warning to handle the case where token fetch itself threw.

    Do NOT add a backfill migration or touch historical pool_stats_history rows (D-20 — natural overwrite only). Do NOT add automatic stale reset (D-16 — manual DB reset only; the JSDoc on markPoolStale from Plan 02 already documents this). Do NOT change `startFeeUpdaterCron`, `stopFeeUpdaterCron`, `getCronStatus`, or the 5-minute `*/5 * * * *` schedule (D-18/D-19). Do NOT change the singleton `cronJob` module-state pattern. Do NOT edit `polling-strategy.ts` (no change needed).
  </read_first>
  <verify>
    <automated>npx vitest run tests/cron/fee-updater.test.ts --reporter=dot && npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "1e9" lib/cron/fee-updater.ts` returns 0 (no SOL-lamports conversion remains — CRON-02 regression guard)
    - `grep -c "USD_MICROUNITS\|1_000_000\|1e6" lib/cron/fee-updater.ts` returns >=1 (microunits conversion present)
    - `grep -c "cumulative_metrics" lib/cron/fee-updater.ts` returns >=1 (uses cumulative_metrics.fees per D-08)
    - `grep -c "farm_apr" lib/cron/fee-updater.ts` returns >=1 (APR field per D-03)
    - `grep -c "feeTokenMode" lib/cron/fee-updater.ts` returns >=1 (CRON-01 mode read)
    - `grep -c "bothTokens" lib/cron/fee-updater.ts` returns >=1 (CRON-03 both-token awareness)
    - `grep -c "markPoolStale" lib/cron/fee-updater.ts` returns >=2 (circuit breaker in BOTH failure branches)
    - `grep -c "MAX_CONSECUTIVE_FAILURES" lib/cron/fee-updater.ts` returns >=2 (threshold check in both branches + import)
    - `grep -c "getFeeUpdateSchedule" lib/cron/fee-updater.ts` returns >=2 (re-fetch in both branches per Pitfall 4)
    - `grep -c "lp_fee30d\|lp_fee24h\|volume24h *\\*" lib/cron/fee-updater.ts` returns 0 (old non-existent fields gone)
    - `npx vitest run tests/cron/fee-updater.test.ts` exits 0 (all cron tests GREEN)
    - `npm run type-check` passes
    - The 5-minute cron schedule string `*/5 * * * *` is unchanged
    - `startFeeUpdaterCron`, `stopFeeUpdaterCron`, `getCronStatus` functions are unchanged
  </acceptance_criteria>
  <done>The cron reads feeTokenMode, converts to USD microunits with no * 1e9, treats both-token pools identically to quote-only with a deferred-per-side log, and triggers markPoolStale via the re-fetch threshold check in both failure branches. All cron tests GREEN; type-check passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Meteora API → cron | External USD floats cross here — defensively convert with `?? 0` before storage |
| cron → SQLite | Trusted internal; tokenId/poolAddress are DB-sourced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | Tampering / Integrity | USD→microunits conversion bugs could misrepresent fees | mitigate | `Math.floor(usd * 1e6)` with `?? 0` guards on optional fields; regression guard test asserts no `* 1e9`; boundary tests assert `277672.23 → "277672230000"` and `undefined → "0"` (not "NaN"). |
| T-06-06 | DoS | Cron hammering chronically-failing pools | mitigate | Circuit breaker: after `recordUpdateFailure`, re-fetch `getFeeUpdateSchedule`; if `consecutiveFailures >= MAX_CONSECUTIVE_FAILURES (10)`, call `markPoolStale(tokenId)` so `getPoolsDueForUpdate` excludes it (D-14/D-15). Off-by-one prevented by re-fetching post-increment count (Pitfall 4). |
| T-06-07 | Repudiation | Silent cron failure (no alerting) | accept | `[Cron] ✗`/`⚠️` log markers + `consecutiveFailures`/`lastError`/`stale` in DB provide observability. External alerting out of scope for v1. |
| T-06-08 | Tampering | Historical lamport-snapshot vs new USD-microunits unit mismatch in charts | accept | Known accepted tradeoff per D-20 (natural overwrite, no backfill). Pre-Phase-6 pool_stats_history rows stay in lamports; new rows are USD microunits. Display-layer handling is out of scope for Phase 6. Documented in SUMMARY for the display phase. |
</threat_model>

<verification>
- `npx vitest run tests/cron/fee-updater.test.ts` exits 0 (all cron behaviors GREEN)
- `npm run type-check` passes
- `grep -c "1e9" lib/cron/fee-updater.ts` returns 0 (the bug is gone)
- Full suite: `npx vitest run` exits 0 (all three test files GREEN — client, db, cron)
- `npm run lint` passes (no new ESLint errors)
</verification>

<success_criteria>
- CRON-01: cron reads `token.feeTokenMode` from DB per pool and logs it (verified by cron test + grep for `feeTokenMode`)
- CRON-02: quote-only pools tracked with correct USD microunits, no `* 1e9`, using `cumulative_metrics.fees` (verified by boundary test + regression grep)
- CRON-03: both-token pools follow the identical aggregate-USD path with a deferred-per-side log warning (verified by equivalence test)
- Circuit breaker: chronic failures (>=10) call markPoolStale (verified by threshold test)
- Full test suite GREEN; type-check and lint pass
</success_criteria>

<output>
Create `.planning/phases/06-background-jobs-hardening/06-03-SUMMARY.md` when done
</output>